<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.application">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".page.base.datePicker.DatePickerView" />
        <activity android:name=".page.base.tableView.TableView" />
        <activity android:name=".page.base.timePicker.TimePickerView" />
        <activity android:name=".page.base.customView.CustomView" />
        <activity android:name=".page.base.radioGroupView.RadioGroupView" />
        <activity android:name=".page.base.javaShowUI.JavaShowUI" />
        <activity android:name=".page.base.timeInterval.TimeInterval" />
        <activity android:name=".page.senior.progressView.ProgressView" />
        <activity android:name=".page.senior.imageSwiper.ImageSwiper" />
    </application>

</manifest>